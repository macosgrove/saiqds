<%= form_for(@quartet) do |f| %>
  <% if @quartet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quartet.errors.count, "error") %> prohibited this quartet from being saved:</h2>

      <ul>
      <% @quartet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
